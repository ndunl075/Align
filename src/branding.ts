/** In-app copy. Keep `index.html` meta/title in sync for link previews (crawlers read static HTML). */
export const BRAND = {
  name: 'Align',
  /** Subtitle under the logo */
  tagline: 'Stop polling the group chat.',
  /** Browser tab + default Open Graph title */
  shareTitle: 'Align — Stop Polling the Group Chat',
  metaDescription:
    'Plan together with one link. Mark free or busy on proposed times, see a group availability heatmap, and coordinate without drowning in replies.',
} as const
